[build]
  command = "pnpm install && pnpm --filter @myceo/web build"
  publish = "apps/web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  PNPM_VERSION = "8.15.0"